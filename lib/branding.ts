/** Default name when site config has no custom platform title. */
export const DEFAULT_PLATFORM_NAME = "The Thesis Desk";

/** Compact wordmark for mobile header (no legacy product names). */
export const MOBILE_HEADER_WORDMARK = "THESIS DESK";
